platform :ios, '13.0'

target 'SwiftfulFirebaseBootcamp' do
  use_frameworks!

  # Firebase dependencies
  pod 'FirebaseCore'
  pod 'FirebaseAppCheck'
  pod 'FirebaseAppCheckDebugProvider' # for development/debug builds

end
